import { AjaxQuery } from './query';
import { TimeTrackerState } from '../intrerfaces';

export function UserLoginQuery(state: { reducer: TimeTrackerState }) {
    interface Structure {
        data: {
            
        }
    }
}