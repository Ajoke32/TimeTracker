import React from 'react';

const H4 = ({ value }: { value: string }) => {
    return (
        <div>
            <h4 className="heading-text__h4">{value}</h4>
        </div>
    );
};

export default H4;